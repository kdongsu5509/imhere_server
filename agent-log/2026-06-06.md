# 2026-06-06

## Task 1 -- 11:12 KST | architecture

### User Request

좋다. "WebIntegrationTestSupport" 가 좋은 것 같네.

### Questions Asked

- **Q:** 없음

### Agent Plan

1. Current test support classes and their actual usage were re-scanned.
2. A shared `WebIntegrationTestSupport` base was introduced for controller integration tests.
3. Existing controller integration tests were migrated to the new base and obsolete support files were removed.
4. Supporting documentation and ADR were updated to record the decision.

### Work Done

- Added `src/test/kotlin/com/common/testsupport/WebIntegrationTestSupport.kt` with shared `MockMvc` and RestDocs initialization.
- Migrated auth and friends controller integration tests to extend `WebIntegrationTestSupport`.
- Removed the obsolete `AuthIntegrationTestSupport.kt` and the commented `ControllerTestSupport.kt`.
- Added an ADR describing the shared web integration test support decision.
- Updated the `testing-guide` skill with the shared web harness rule.

### Failures & Root Causes

| Attempt | Failure Cause |
|------|---------|
| Updating `testing-guide` with a contextual patch | The file's encoding and existing text made the first context-based patch fail. |

### Fixes Applied

- Appended the new rule directly to `testing-guide` after the existing content instead of matching the broken context.

### Files Changed

| Action | File |
|--------|------|
| created | `src/test/kotlin/com/common/testsupport/WebIntegrationTestSupport.kt` |
| deleted | `src/test/kotlin/com/common/testsupport/ControllerTestSupport.kt` |
| deleted | `src/test/kotlin/com/kdongsu5509/auth/adapter/in/web/AuthIntegrationTestSupport.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/auth/adapter/in/web/LoginControllerIntegrationTest.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/auth/adapter/in/web/RegistrationControllerIntegrationTest.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/auth/adapter/in/web/RefreshControllerIntegrationTest.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/auth/adapter/in/web/UserActivationControllerIntegrationTest.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/friends/controller/FriendRequestControllerIntegrationTest.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/friends/controller/FriendRestrictionControllerIntegrationTest.kt` |
| modified | `src/test/kotlin/com/kdongsu5509/friends/controller/FriendshipControllerIntegrationTest.kt` |
| modified | `.agents/skills/testing-guide/SKILL.md` |
| created | `agent-log/ADR/2026-06-06-web-integration-test-support.md` |

### Skill Updates

| Skill | Action | Insight |
|-------|--------|---------|
| testing-guide | updated | Shared web integration test setup should live in `WebIntegrationTestSupport`, while individual tests only declare scenario-specific mocks and assertions. |

## Task 2 -- 11:38 KST | bugfix

### User Request

ㄱㄱㄱㄱ

### Questions Asked

- **Q:** 없음

### Agent Plan

1. Rebuild the broken `auth/adapter/in/web` integration test file with UTF-8-safe content.
2. Keep the whitelist and token-fixture fixes in place so the tests reach the controller layer.
3. Record the new auth/testing rules that were confirmed during the fix.

### Work Done

- Rewrote `UserActivationControllerIntegrationTest.kt` from scratch with clean ASCII text and correct token setup.
- Kept the public auth endpoint whitelist aligned with the integration test URLs in `application-test.yml`.
- Kept the JWT filter change that converts `ImHereBaseException` into an error response instead of leaking it.
- Added skill rules for public auth paths in tests and for avoiding `responseFields(...)` on empty 401 responses.

### Failures & Root Causes

| Attempt | Failure Cause |
|------|---------|
| Partial patching of the corrupted activation test file | The file had already been damaged by locale-encoded text, so line-based replacement kept leaving broken blocks behind. |
| Running Gradle tests after the auth fix | `build.gradle` has a pre-existing `openapi3` DSL change that triggers `LinkedHashMap cannot be cast to Closure` during project evaluation. |

### Fixes Applied

- Replaced the entire activation integration test file instead of trying to patch individual corrupted sections.
- Kept new content ASCII-only to avoid another encoding round-trip.

### Files Changed

| Action | File |
|--------|------|
| modified | `src/test/kotlin/com/kdongsu5509/auth/adapter/in/web/UserActivationControllerIntegrationTest.kt` |
| modified | `src/test/resources/application-test.yml` |
| modified | `src/main/kotlin/com/kdongsu5509/auth/security/filter/JwtAuthenticationFilter.kt` |
| modified | `.agents/skills/security-auth-rules/SKILL.md` |
| modified | `.agents/skills/testing-guide/SKILL.md` |

### Skill Updates

| Skill | Action | Insight |
|-------|--------|---------|
| security-auth-rules | updated | Integration tests for public auth endpoints must whitelist the same URLs that the controllers expose, otherwise requests die in the security filter chain. |
| testing-guide | updated | Do not document empty 401 responses with `responseFields(...)`; only document a body when the filter/controller actually writes one. |
